# GEMINI.md - ねこの相棒とふしぎの島 開発ガイドライン

このドキュメントは、Google Antigravity (Gemini) が本プロジェクトを理解し、一貫した方針で開発・運用するための共通ガイドラインです。

---

## 1. プロジェクト概要

- タイトル: ねこの相棒とふしぎの島
- 目的: 8才の女の子といっしょに作る、ブラウザで遊べる探索アドベンチャーゲーム
- 対象プレイヤー: 8才（難易度は小6相当、詰まったら自動でやさしくなる）
- 基本ルール:
  - リイコを動かす: 画面の方向ボタン、矢印キー、WASD
  - 剣を振る: 画面右下の🗡️ボタン、Jキー、スペースキー
  - 仲間ねこ（ミィ・3面で加入）: 敵や地面をタップすると指示できる
  - 会話を進める: 画面タップ、スペースキー、エンターキー
  - 進行: 各面を突破して最終的に北の「やみのしろ」を目指す（全4面構成）

---

## 2. 全体構成とフェーズ設計

「1つの面で、増える遊び方は1つだけ」がゲームデザインの背骨です。

- 1面（はじまりの森）: 剣（ひとり）。操作を覚え、さらわれた友だちハナを助けに行く動機を作る。
- 2面（ささやきの谷）: 仲間1 妖精ベルが加入（ヒント・回復）。謎解きが本格化する。
- 3面（静寂の湖）: 仲間2 猫ミィが加入（タップ戦闘）＋ 魔法の習得（連携と技による謎解き）。
- 4面（やみのしろ）: 総仕上げ。全アクションを活用した魔王城とボス戦。

---

## 3. 面白さの設計原則（抜粋）

詳細は docs/GAME_PLAN.md を参照。

- 原則A [目標が常に見えている]: 「お城に行く」「ハナを助ける」を常に見失わせない。
- 原則B [自分で気づいた感]: ヒントは答えではなく「見る場所」を教える。
- 原則C [段階的な手応え]: 練習（かかし）→ 応用（ザコ）→ 本番（突進敵など）の順に出す。
- 原則D [触ったときの気持ちよさ]: ヒットストップ、ノックバック、画面揺れ、合成効果音。
- 原則E [失敗が安い]: やられても直前のチェックポイントから即時復帰。進行状況や宝箱は保持。
- 原則F [仲間との一体感]: 勝手にしゃべる、ピンチに薬を持ってくる、置いてけぼりにしない。

---

## 4. 技術仕様と制約事項

- 言語・環境: HTML5, CSS3, Vanilla JavaScript (ES6+), Canvas 2D API
- 外部依存: なし（フレームワークや外部CDNライブラリは使用しない）
- サウンド: WebAudio API による内部波形合成（外部音声ファイル不要）
- キャラクター画像仕様:
  - 128×128ピクセル、背景透過PNG
  - 下揃え配置（足の裏が画像下端から4%の位置）
  - 4方向×2コマ（down1/2, up1/2, left1/2, right1/2）
  - 画像読み込み失敗時は自動的に代替絵文字で描画（フォールバック）
- マップパーツ仕様:
  - assets/parts/*.png
  - tools/build-assets.py でスプライトシートから透過切り出し
  - r=0 のパーツは衝突しない飾り（草・小石など）

---

## 5. ファイル構成と役割

- index.html: ゲーム本体のHTML枠組み
- css/style.css: UIスタイル定義
- js/engine.js: ゲームエンジン（描画、当たり判定、ゲームループ、入力）
- js/scenario.js: 全体設定（あいことば、プレイヤー・敵のグラフィック定義、初期面設定）
- js/stages/stage1.js, stage2.js: 各面のシナリオ（会話、進行フラグ、演出）
- js/maps/stage1.js, stage2.js: 各面のマップデータ（地形、配置イベント）
- js/mapdata.js: マップおよびイベントのデータ構造定義
- js/assets-data.js: 自動生成されるマップパーツ定義
- js/assets.js: パーツ画像の描画・管理
- tools/map-editor.html, tools/map-editor.js, tools/map-editor.css: マップ作成ツール（地形＋イベント配置）
- tools/build-assets.py: スプライトシート切り出しスクリプト
- tests/: Node.jsによるテスト群（DOMシミュレータ同梱）
- docs/: 設計書（GAME_PLAN.md, FUN-CHECKLIST.md, TASKS.md）

---

## 6. テストの実行方法

Windows環境（PowerShell）では以下のコマンドで全テストを一括実行できます:

```powershell
Get-ChildItem tests/test-*.js | ForEach-Object { Write-Host "=== $($_.Name) ==="; node $_.FullName }
```

個別テスト:
- tests/test-map-editor.js: マップエディタ動作確認
- tests/test-assets.js: パーツ画像整合性
- tests/test-game.js: 基本ゲームエンジン動作
- tests/test-stage1-map.js: 1面の地形到達性・抜け道検証
- tests/test-battle.js: 戦闘バランス、コンボ、敵AI
- tests/test-save.js: セーブ/ロード、フラグ、カットシーン、ヒント
- tests/test-fairy.js: 妖精ベルの加入、追従、ヒント、回復

コードを変更した際は、必ずテストを実行して全項目が合格することを確認してください。

---

## 7. 開発ルール

- ドキュメントや説明文、回答は日本語で記述する。
- ユーザー指示のない限り、強調記号にアスタリスク2つ（太字）は使用しない。
- 仕様変更や機能追加を行う際は、docs/GAME_PLAN.md および docs/TASKS.md との整合性を維持する。
- 常にテストを書いて動作を検証する。
